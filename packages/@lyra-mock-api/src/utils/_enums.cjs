// @ts-check

const ACTIONS = ['toggle', 'input'];
const SCENARIOS = ['matched', 'difference'];
const OPERATORS = ['equals'];
const TRANSPORT = ['http','socket','event'];

module.exports = { ACTIONS, SCENARIOS, OPERATORS, TRANSPORT };
